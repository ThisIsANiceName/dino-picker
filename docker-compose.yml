services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - dino_data:/data
    environment:
      NODE_ENV: production
      PORT: 3001
      DATA_DIR: /data
    restart: unless-stopped

volumes:
  dino_data:
