services:
  app:
    build: .
    ports:
      - "8091:8091"
    volumes:
      - ./data:/data
    environment:
      NODE_ENV: production
      PORT: 8091
      DATA_DIR: /data
    restart: unless-stopped
